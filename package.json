{
  "name": "@yeaft/webchat",
  "version": "1.0.0",
  "description": "Web-based multi-provider code agent platform with native Yeaft Code Agent, Claude Code CLI, and GitHub Copilot CLI backends",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "agent"
  ],
  "scripts": {
    "test": "npm run test:budget && vitest run --exclude '**/.claude/worktrees/**' --exclude '**/.worktrees/**' --exclude '**/.yeaft/worktrees/**'",
    "test:budget": "node scripts/check-test-budget.mjs",
    "test:focus": "vitest run --config vitest.focus.config.js",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test --config e2e/playwright.config.js",
    "test:e2e:ui": "playwright test --config e2e/playwright.config.js --ui",
    "test:e2e:smoke": "playwright test --config e2e/playwright.config.js --project=smoke",
    "test:all": "npm run test && npm run test:e2e",
    "install:all": "npm install --workspaces",
    "build": "node web/build.js",
    "check:server-agent-syntax": "node scripts/check-server-agent-syntax.mjs",
    "smoke:server-startup": "node scripts/smoke-server-startup.mjs",
    "release:guard": "npm run check:server-agent-syntax && npm run smoke:server-startup",
    "avatars": "node scripts/generate-avatars.mjs",
    "start:server": "npm run start --workspace=server",
    "start:server:prod": "cross-env SERVE_DIST=true npm run start --workspace=server",
    "start:agent": "npm run start --workspace=agent",
    "dev:server": "npm run dev --workspace=server",
    "dev:agent": "npm run dev --workspace=agent",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:agent\"",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:screenshots": "node scripts/capture-doc-screenshots.mjs",
    "docs:screenshots:verify-failures": "node scripts/verify-doc-screenshot-failures.mjs"
  },
  "devDependencies": {
    "@dicebear/collection": "^9.4.2",
    "@dicebear/core": "^9.4.2",
    "@playwright/test": "^1.58.2",
    "@vue/test-utils": "^2.4.11",
    "concurrently": "9.2.4",
    "cross-env": "^7.0.3",
    "esbuild": "^0.28.1",
    "happy-dom": "^20.9.0",
    "supertest": "^7.2.2",
    "vitepress": "2.0.0-alpha.6",
    "vitest": "^4.1.10",
    "vue": "^3.5.32"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yeaft/claude-web-chat.git"
  },
  "homepage": "https://github.com/yeaft/claude-web-chat",
  "bugs": {
    "url": "https://github.com/yeaft/claude-web-chat/issues"
  },
  "keywords": [
    "claude",
    "cli",
    "websocket",
    "remote",
    "yeaft",
    "webchat"
  ],
  "license": "MIT",
  "author": "Yeaft",
  "engines": {
    "node": ">=22.5.0"
  },
  "overrides": {
    "js-cookie": "^3.0.8"
  }
}
