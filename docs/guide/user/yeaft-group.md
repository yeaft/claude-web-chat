# Yeaft Sessions

This URL is retained for compatibility with older bookmarks. The product no longer uses “group mode” as a native-engine concept.

A Yeaft **Session** is the only native conversation and collaboration unit. It can contain one VP for focused work or several VPs for parallel collaboration.

Continue to [Yeaft Sessions and Projects](./yeaft-session.md).
